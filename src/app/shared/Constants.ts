import { environment } from '../../environments/environment';

export class Constants {
    public static URL_FRONT = environment.FRONT_URL;
    public static URL_BACK = environment.BACK_URL;
    public static URL_BACK_API = environment.BACK_URL_API;
}